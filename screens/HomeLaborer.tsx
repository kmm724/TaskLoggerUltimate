import React from 'react';
import { View, Text } from 'react-native';

const HomeLaborer: React.FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Laborer Dashboard</Text>
    </View>
  );
};

export default HomeLaborer;
